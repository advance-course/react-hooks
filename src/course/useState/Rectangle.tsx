import React, { useState } from 'react';

export default function Rectangle() {
  const [width, setWidth] = useState(100);
  const [height, setHeight] = useState(100);

  return (
    <div>
      <div className="reatangle" />
      
    </div>
  )
}